To build sphinx docs:
```
pip install cloud_sptheme
cd lectures
sphinx-build . _build
```

Open _build/index.html in a browser.

To  build PDF from LaTeX
```
sphinx-build . _build -b latex
cd _build
pdflatex ComputationalStatisticsinPython.tex
cd ..
```

Open _build/ComputationalStatisticsinPython.pdf
